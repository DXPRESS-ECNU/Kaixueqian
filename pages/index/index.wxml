<!--index.wxml-->

<view class="container">
  <view class="card-grid">
    <view wx:for="{{CONFIG}}" wx:for-item="card" wx:for-index="idx" class="grid-item">
      <image class="card-image" src="{{URL_PREFIX + card.titleimage}}" />
    </view>
  </view>
  <view class="footer">
  </view>
</view>
